[project]
name = "evals"
version = "0.1.0"
description = "Evaluation suite for Vanshika AI Persona — hallucination, retrieval, and voice metrics"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "groq",
    "httpx",
    "python-dotenv",
    "google-genai>=2.8.0",
    "supabase",
]
